// Generated: minimal FontGuard — expand when you want to load fonts
import type { ReactNode } from 'react'

type Props = { children: ReactNode }

export default function FontGuard({ children }: Props) {
	// Placeholder: do not load fonts automatically. Add font-loading when requested.
	return <>{children}</>
}
